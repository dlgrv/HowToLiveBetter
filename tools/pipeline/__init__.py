"""Reusable pipeline core: project config, verdict store, judge backends.

Engine code here knows nothing about the htlb book specifically — all
project facts (languages, paths, models) come from tools/rules/project.yaml.
"""
